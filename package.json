{
  "name": "touchai",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["sdk"],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node scripts/serve.mjs dist",
    "docker:build": "docker build -t touchai .",
    "docker:run": "docker run --rm -p 4173:4173 touchai",
    "deploy:check": "npm run build && node scripts/serve.mjs dist"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "touchai-sdk": "0.4.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
