{
  "name": "touchai",
  "private": true,
  "workspaces": [
    "packages/touch-spec",
    "packages/touch-runtime",
    "packages/touch-dataset"
  ],
  "scripts": {
    "build": "npm run build -w @touchai/touch-spec && npm run build -w @touchai/touch-runtime && npm run build -w @touchai/touch-dataset",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm exec -w @touchai/touch-spec -- tsc --noEmit && npm run build -w @touchai/touch-spec && npm exec -w @touchai/touch-runtime -- tsc --noEmit && npm exec -w @touchai/touch-dataset -- tsc --noEmit"
  },
  "engines": {
    "node": "20.x"
  }
}
