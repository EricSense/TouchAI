{
  "name": "forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FORGE — an AI-native fund that detects problems, instantiates disposable ventures, and gets repaid as a share of verified outcomes.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
